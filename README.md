# Savior


## Installation
./bin/setup

## Development
After checking out the repo or unzip the file, run `bin/setup` to install dependencies. Then, run `rake spec` to run the tests. You can access your custom input in terminal just run `bin/run`. You can also run `bin/console` for an interactive prompt that will allow you to experiment.
